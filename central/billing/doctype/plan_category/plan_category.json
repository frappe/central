{
 "actions": [],
 "autoname": "field:category_name",
 "creation": "2026-06-23 00:00:00.000000",
 "description": "A product family (VM Plans, AI Tokens, SaaS Storage, Remote Storage). Behavioral and self-describing: it declares the resource types a member plan may include, its sub-category axis, and which configurator builder authors it. Adding a family is a new document, not a schema change (ADR 0007).",
 "doctype": "DocType",
 "editable_grid": 1,
 "engine": "InnoDB",
 "field_order": [
  "category_name",
  "is_active",
  "description",
  "column_break_head",
  "configurator_builder",
  "provision_target",
  "sub_category_label",
  "section_break_billing",
  "billing_type",
  "billing_interval",
  "column_break_billing",
  "pricing_mode",
  "settlement_mode",
  "reporting_mode",
  "section_break_resources",
  "allowed_resource_types"
 ],
 "fields": [
  {
   "fieldname": "category_name",
   "fieldtype": "Data",
   "in_list_view": 1,
   "label": "Category",
   "reqd": 1,
   "unique": 1
  },
  {
   "default": "1",
   "fieldname": "is_active",
   "fieldtype": "Check",
   "in_list_view": 1,
   "label": "Is Active"
  },
  {
   "fieldname": "description",
   "fieldtype": "Small Text",
   "label": "Description"
  },
  {
   "fieldname": "column_break_head",
   "fieldtype": "Column Break"
  },
  {
   "default": "Simple",
   "fieldname": "configurator_builder",
   "fieldtype": "Select",
   "label": "Configurator Builder",
   "options": "VM Rungs\nSimple",
   "reqd": 1,
   "description": "Which Plan Configurator builder authors this category. VM Rungs = the vCPU/memory/disk/transfer ladder; Simple = name + included quantity + rate."
  },
  {
   "fieldname": "provision_target",
   "fieldtype": "Select",
   "label": "Provision Target",
   "options": "\nServer\nResource",
   "description": "What a member plan provisions. 'Server' = a whole sellable machine, offered in the create-server flow (the Configurator builds a ladder of plans). 'Resource' = individual resource types (Compute / Memory / Disk) priced per unit, so custom configs can be composed and billed (the Configurator authors Resource Rates). Blank = not directly provisionable (e.g. metered AI Tokens, storage subscriptions)."
  },
  {
   "fieldname": "sub_category_label",
   "fieldtype": "Data",
   "label": "Sub-Category Label",
   "description": "What a sub-category means for this family (e.g. 'Optimization profile', 'Storage purpose'). Blank = this family has no sub-category axis; plans attach directly to the category."
  },
  {
   "fieldname": "section_break_billing",
   "fieldtype": "Section Break",
   "label": "Billing Behaviour"
  },
  {
   "default": "Fixed",
   "fieldname": "billing_type",
   "fieldtype": "Select",
   "label": "Billing Type",
   "options": "Fixed\nMetered",
   "reqd": 1,
   "description": "Fixed = a flat rate per billing cycle (a bundle). Metered = per-unit usage billing; a member plan with a single resource is a metering target resolved by its resource type (ADR 0008)."
  },
  {
   "fieldname": "billing_interval",
   "fieldtype": "Select",
   "label": "Billing Interval",
   "options": "\nHourly\nDaily\nMonthly",
   "description": "The metering cadence for a Metered family. Leave blank for Fixed families."
  },
  {
   "fieldname": "column_break_billing",
   "fieldtype": "Column Break"
  },
  {
   "fieldname": "pricing_mode",
   "fieldtype": "Select",
   "label": "Pricing Mode",
   "options": "\nGrandfathered\nLive",
   "description": "For a Metered family: Grandfathered bills the rate locked at provision (like bundles); Live reads the current Catalog Rate each period with no allowance, for depreciating storage (ADR 0002). Leave blank for Fixed families."
  },
  {
   "fieldname": "settlement_mode",
   "fieldtype": "Select",
   "label": "Settlement Mode",
   "options": "\nPostpaid Overage\nPrepaid Pack",
   "description": "What the included allowance means when it runs out (ADR 0015). Postpaid Overage (default): keep serving, bill max(0, qty-allowance) x rate at period close. Prepaid Pack: the allowance is a purchased balance drawn down as usage lands; blocked/degraded at zero until another pack is bought. Blank = Postpaid Overage."
  },
  {
   "fieldname": "reporting_mode",
   "fieldtype": "Select",
   "label": "Reporting Mode",
   "options": "\nAuthoritative\nIncremental",
   "description": "How a consumer service reports usage (ADR 0015). Authoritative (default): the service sends the period's running total; Central replaces the rollup. Incremental: the service posts deltas as consumed; Central accumulates and dedupes by a monotonic sequence cursor (assumes ordered delivery). Blank = Authoritative."
  },
  {
   "fieldname": "section_break_resources",
   "fieldtype": "Section Break",
   "label": "Allowed Resource Types"
  },
  {
   "fieldname": "allowed_resource_types",
   "fieldtype": "Table",
   "label": "Allowed Resource Types",
   "options": "Plan Category Resource Type",
   "description": "A member plan's composition may only use these resource types. Blank = unconstrained."
  }
 ],
 "index_web_pages_for_search": 1,
 "links": [],
 "modified": "2026-06-23 00:00:00.000000",
 "modified_by": "Administrator",
 "module": "Billing",
 "name": "Plan Category",
 "naming_rule": "By fieldname",
 "owner": "Administrator",
 "permissions": [
  {
   "create": 1,
   "delete": 1,
   "email": 1,
   "export": 1,
   "print": 1,
   "read": 1,
   "report": 1,
   "role": "System Manager",
   "share": 1,
   "write": 1
  }
 ],
 "row_format": "Dynamic",
 "sort_field": "creation",
 "sort_order": "DESC",
 "states": [],
 "track_changes": 1
}
